#pragma once
#include <assimp/scene.h>

#include "assimp/Importer.hpp"
#include "assimp/postprocess.h"

#include "Model.h"

namespace ToToEng
{
    // class ModelImporter 
    // {
    // private:
    //     static void loadNode(Model* model, aiNode* node, const aiScene* scene);
    //     static void loadMesh(aiMesh* mesh, const aiScene* scene);
    //     static void loadMaterials(const aiScene* scene, std::string diffusePath, std::string specularPath);
    //
    // public:
    //     static Model* importModel(Renderer* renderer, std::string modelPath);
    //     static Model* importModel(Renderer* renderer, std::string modelPath, std::string diffusePath, std::string specularPath);
    // };
}